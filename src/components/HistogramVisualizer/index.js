import React from 'react';
import s from './styles.css';

const HistogramVisualizer = () => (
  <div className={s.histogramvisualizercontainer}>
    <h1>Histogram Visualizer</h1>
  </div>
);

export default HistogramVisualizer;
